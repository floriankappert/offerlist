<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : "OfferList" %></title>
  
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : 'OfferList is the hottest shit for listing offers.' %>" >
    
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	
	<div class="row">
		<div class="col-md-2">
			<ul class="nav nav-pills nav-stacked">
			  <li><%= link_to "Offers", offers_path %></li>
			</ul>
		</div>
		<div class="col-md-8">
			<%= yield %>
		</div>
	</div>


</body>
</html>
